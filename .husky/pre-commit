if git diff --cached --name-only | grep -qE '\.(js|jsx|ts|tsx|json|css)$'; then
  STAGED=$(git diff --cached --name-only | grep -E '\.(js|jsx|ts|tsx|json|css)$')
  npx biome check --write --staged
  echo "$STAGED" | xargs git add
fi
